#ifndef CLEANSTRING_H
#define CLEANSTRING_H

#include <iostream>
#include <string>
#include <ctype.h>

std::string cleanString(std::string);
bool examinString(std::string, char);
std::string clnFileName(std::string);
std::string apndFileEx(std::string, std::string);

#endif